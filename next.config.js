/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["www.focusclass.in/"],
    },
    output: "standalone",
    reactStrictMode: false,
}

module.exports = nextConfig
