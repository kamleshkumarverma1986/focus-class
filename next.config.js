/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["www.focusclass.in/", "images.unsplash.com", "media.geeksforgeeks.org"],
    },
}

module.exports = nextConfig
